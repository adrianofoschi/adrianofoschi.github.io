---
interface Props {
	date: Date;
}

import { isoDate } from '../utils/date';

const { date } = Astro.props;
---

<time datetime={isoDate(date)} class="font-mono">
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
