# One interface, two backends — the post's own description of the persistence layer.
direction: right
stores: "every store\n\nsaves and reloads through\none common interface"
ordinary: "ordinary data\nthe phone's normal storage"
secrets: "keys and password\nthe OS encrypted keystore"
stores -> ordinary: same code
stores -> secrets: same code
